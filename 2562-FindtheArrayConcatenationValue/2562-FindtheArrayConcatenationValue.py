                result = a + b
                print(result)
                concat = int(result)
                a = ""
                b = str(nums.pop()) 
            if len(nums) >0:
        concat_value = 0
        while len(nums) > 0:
            a = str(nums.pop(0))
    def findTheArrayConcVal(self, nums: List[int]) -> int:  
        concat = 0  
        if len(nums)==1:
class Solution:
            return nums[0]
                b = ""
                result = ""

                concat_value += concat
                concat = 0
            if len(nums) == 1:
                concat_value += nums[0]
              
            
        return concat_value
[
